#include<bits/stdc++.h>
using namespace std;

void unorderedSetDemo(){
    /**
     * Stores unique, but the order is randomized (not sorted)
     * lower_bound() and upper_bound() function does not work
     * rest all functions are same as others
     * It does not stores in any particular order it has a better 
     * complexity than set in most cases, except some when collision happens
     * 
     */
}

int main(){
    unorderedSetDemo();
    return 0;
}